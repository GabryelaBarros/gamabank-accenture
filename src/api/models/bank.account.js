class BankAccount {
    constructor({ balance, userId, credit, receipt }) {
        this.balance = 0
        this.userId = userId
        this.credit = 200
        this.receipt = 0
    }
}

module.exports = BankAccount